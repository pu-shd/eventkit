# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Test and coverage artefacts
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.ruff_cache/
.mypy_cache/

# Node (test-only, arrives with eventkit.ui)
node_modules/

# Deployment state
#
# The .eventkit/state.json step ledger IS committed on purpose — it is the record
# of what was provisioned. Local working values, including secrets, are not.
.env
.env.deploy
.env.local
*.db
*.db.pre-*.bak

# OS
.DS_Store
