#!/bin/sh
# Mock gh. Records the call; silent success unless a fixture says otherwise.
: "${AZ_MOCK_LOG:=/tmp/az-mock.log}"
printf '%s %s\n' "gh" "$*" >> "$AZ_MOCK_LOG"
if [ -n "${GH_MOCK_OUT:-}" ]; then printf '%s' "${GH_MOCK_OUT}"; fi
if [ -n "${GH_MOCK_FAIL:-}" ]; then exit 1; fi
exit 0
