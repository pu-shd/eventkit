#!/bin/sh
# Mock dig. Records the call; silent success unless a fixture says otherwise.
: "${AZ_MOCK_LOG:=/tmp/az-mock.log}"
printf '%s %s\n' "dig" "$*" >> "$AZ_MOCK_LOG"
if [ -n "${DIG_MOCK_OUT:-}" ]; then printf '%s' "${DIG_MOCK_OUT}"; fi
if [ -n "${DIG_MOCK_FAIL:-}" ]; then exit 1; fi
exit 0
